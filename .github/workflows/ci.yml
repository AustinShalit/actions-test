name: CI

on:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: home
        run: echo $HOME
      - name: home
        run: cd ~/ && echo $(pwd)

